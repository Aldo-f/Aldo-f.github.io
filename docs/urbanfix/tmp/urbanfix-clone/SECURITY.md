# Security reminder — keys must be loaded from environment
# Real keys are NOT in storage.ts (partial fragments only shown for ref).
# Load from: FREELLM_API_KEY, GOOGLE_AI_API_KEY, OPENROUTER_API_KEY, OPENAI_API_KEY, GROQ_API_KEY
# See .env.example for variable names.
# NEVER commit .env files.
